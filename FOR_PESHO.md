# minSHmap — за Пешо

Минимална, четима реализация на sketch-based read mapper (в стила на minSH).
Един файл [minshmap.py](minshmap.py); [minshmap.cpp](minshmap.cpp) е същият
алгоритъм, бит-в-бит същия изход, за реален мащаб.

## Алгоритъмът (три стъпки)

1. **Sketch — canonical (w, k)-minimizers.** Не пишем свой скеч; идва от чужда
   библиотека [`minimizer-iter`](https://crates.io/crates/minimizer-iter)
   (rust-seq / Igor Martayan). За всеки прозорец от `w` k-мера се избира k-мерът с
   най-малък хеш. *Canonical* = рийдът и неговият reverse-complement дават едни и
   същи minimizer-и, така че един индекс обслужва двете вериги. (`w` е нечётно.)

2. **Index.** Минимайзерите на референцията се обръщат в речник
   `hash → [(сегмент, позиция, верига), …]`.

3. **Map.** За всеки рийд: скеч → seed-ове, подредени най-рядък пръв → разпръскване
   в припокриващи се прозорци → оценка на **weighted** containment с **seed-евристиката**
   `sh = 1 − (използвани − съвпадения) / m` (горна граница на постижимото; `sh < θ`
   ⇒ прозорецът се отрязва). *Weighted* = съвпаденията се броят с **кратността** на
   всеки минимайзер в рийда (`used`, `matches`, `m` са в брой поява, не уникални), така
   че рийд, доминиран от повтарящ се k-mer, не се наказва — weighted Jaccard, не set.
   Най-добрият прозорец → ред PAF, плюс качество на мапването `mapq ∈ {0, 60}` по
   **φ-free** правилото (виж по-долу).

Това е цялата идея — ~150 реда, четат се ред по ред. Същата seed-евристика като в
minSH, само че там ограничава edit distance за подравняване, а тук — containment за
намиране на локус.

## Скечът е от библиотека (и за Python, и за C++)

Твоята забележка беше да ползваме чужд minimizer, не copy-paste. Направено:

- **Python** вика библиотеката директно през тънка PyO3 обвивка
  [minimizer_ext/](minimizer_ext/); в [minshmap.py](minshmap.py) остава само:
  ```python
  from minimizer_ext import canonical_minimizers
  def minimizers(seq, k, w):
      for pos, h, strand in canonical_minimizers(seq, k, w):
          yield pos, h, 1 if strand else 0
  ```
- **C++** вика **същия** crate през C ABI (`mz_compute`/`mz_free`), затова двете
  реализации дават **byte-identical** изход (проверено: py == cpp == cpp `-j4`).

## Хешът: достатъчно ли е случаен и има ли колизии?

Твоят въпрос беше две неща: (1) достатъчно случаен ли е хешът и как се сравнява със
стандартен библиотечен хеш; (2) би ли вървял minshmap значително по-бързо с друг хеш.

**Най-важното: индексът НЕ се ключира по хеш.** Проверих какво реално връща
библиотеката като „стойност" на минимайзъра — това е **точният 2-битов код на
каноничния k-mer**, а не хеш. Емпирично (chr21, 1.5 Mbp проба): върнатата стойност
съвпада с моя независимо изчислен каноничен код за **1 499 986 / 1 499 986** позиции,
а максималната стойност е `2^30` (точно `2·k = 30` бита за `k = 15`). Хеш би
запълвал целите `2^64`.

Следствие: за `k ≤ 32` кодът се събира в 64-битов ключ, затова **колизии в индекса
няма по конструкция** — два различни k-mer-а никога не делят един ключ (0 колизии,
проверено и на цял chr21 в предишен тест).

**Тогава каква е ролята на хеша (wyhash по подразбиране в `minimizer-iter`)?** Само
да реши **кой** k-mer в прозореца е минимайзърът (подредбата за семплиране). Тук
„достатъчно случаен" значи „семплира равномерно, без слепвания". Измерено:

| метрика | wyhash (текущ) | xxh3-64 (библ.) | murmur3 (библ.) |
| --- | ---: | ---: | ---: |
| плътност min/база (цел `2/(w+1)=0.1667`) | **0.1669** | — | — |
| `max\|bit−0.5\|` (уникални k-mer) | n/a¹ | 0.0025 | 0.0015 |
| `χ²/df` върху 4096 кофи (цел ≈ 1.0) | n/a¹ | 1.012 | 0.970 |

¹ За wyhash тези тестове не важат, защото върнатата стойност е кодът (30 бита), не
хешът; вътрешният ред обаче произвежда плътност `0.1669 ≈ 0.1667` — практически
идеално равномерно семплиране. Ако въобще пазехме минимайзери **по хеш**, стандартен
библиотечен хеш би изглеждал като колоните xxh3/murmur3 по-горе (равномерен,
`χ²/df ≈ 1`, отклонение на битове `< 0.3 %`) — т.е. текущата схема не отстъпва на
стандартен хеш, а и не разчита на хеш за коректността на индекса.

**Математика на колизиите (birthday bound), ако все пак се ползваше хеш като ключ.**
При хеш-пространство `M = 2^64` и `D` различни ключа, очакваният брой сблъскващи се
двойки е `λ = C(D,2)/M ≈ D²/(2M)`, а `P(≥1 колизия) ≈ 1 − e^(−λ) ≈ λ` (за `D² ≪ M`):

| сценарий | `D` (различни k-mer) | `P(≥1 колизия)` |
| --- | ---: | ---: |
| chr21 минимайзери | 4 954 588 | ≈ 6.7 × 10⁻⁷ |
| WGS минимайзери (T2T, `k15 w31`) | 35 557 851 | ≈ 3.4 × 10⁻⁵ (~1 на 29 000) |
| **всички** канонични 15-mer-и (`4¹⁵/2`) | 536 870 912 | ≈ 7.8 × 10⁻³ |

Прагът за 50 % шанс за **една** колизия е `D ≈ 1.177·√M ≈ 5.06 × 10⁹` (≈ `2^32.2`)
ключа — повече от целия капацитет от канонични 15-mer-и (`5.37 × 10⁸`). Затова при
`k = 15` колизии са практически невъзможни дори при хеш-ключ; а тъй като ключът е
самият код, вероятността е **точно 0**.

**Ще върви ли по-бързо с друг хеш? Не значимо.** Скечът върви ~7.5 Mbp/s (ограничен
от Python/PyO3 маршалирането, не от хеша), а по-ранен тест показа, че хешът/скечът
**не е тясното място** (SIMD върху скеча дава само 10–20 %). wyhash вече е сред
най-бързите хешове; смяна със стандартен библиотечен хеш (или rolling ntHash) в
най-добрия случай изравнява. Тясното място е lookup-ът в индекса и обработката на
seed-овете — затова смяна на хеша няма да ускори minshmap осезаемо.

## Билд и пускане

```sh
# еднократно: minimizer-ите (иска Rust toolchain + maturin)
cd minimizer_ext
maturin build --release -i <python> && pip install target/wheels/*.whl   # за Python
cargo build --release --no-default-features                              # статична либ за C++

# Python
python minshmap.py ref.fa reads.fa -k 15 -w 11 -t 0.9

# C++
g++ -O3 -std=c++17 -march=native -pthread -I ../shmap/ext/unordered_dense/include \
    -o minshmap minshmap.cpp -L minimizer_ext/target/release \
    -l:libminimizer_ext.a -lws2_32 -luserenv -lbcrypt -lntdll
./minshmap ref.fa reads.fa -k 15 -w 11 -t 0.9
```

`-w` е нечётно (canonical). `-j N` в C++ е само базов read-level паралелизъм
(изходът е същият като `-j 1`), не оптимизация.

## Премахване на параметъра φ (Def. 5)

В статията Def. 5 („алтернативни мапвания") използваше свободен параметър **φ**
(максимално допустимо припокриване) плюс неудобната клауза „различна посока".
Премахнахме φ, като мерим припокриването в **оригиналните T-координати** на
референцията (покрития интервал `g(s)`, точно както е изходът на Sec. 4.6): тогава
reverse-complement образът на най-доброто мапване **съвпада** с него (припокриване
= 1) и автоматично отпада — без клаузата за посока и без параметър.

**Дефиниция (φ-free, Def. 5′).** *Алтернатива* = всеки кандидат, чийто референтен
интервал е **дизюнктен** с този на най-доброто. `mapq = 60`, ако всяка дизюнктна
алтернатива е по-слаба с повече от `δ` (default 0.15); иначе `mapq = 0`. Самият
**placement (позицията) е непроменен** — добавя се само mapq.

Двете програмки (py и cpp) смятат това идентично. Пълни дефиниции/формули, лема и
емпирична валидация (вкл. тест с дублиран блок, който коректно сваля mapq→0):
[NOTE_phi_elimination.md](NOTE_phi_elimination.md).

## Последни резултати

Всеки файл с прочити се мапва към **референцията, от която идват**: hifi/ont/clr са
whole-genome прочити на HG002, затова се мапват към **целия човешки геном**
(hs1.fa = T2T-CHM13v2.0); за демонстрация на хромозомен случай извличаме прочитите,
които реално принадлежат на chr21, и ги мапваме към **само chr21**. И трите mapper-а
вървят в една и съща WSL/Linux среда; `shmap` е с изравнена чувствителност
(`-r = 2/(w+1) = 0.0625`, `-m Containment`), `k=15 w=31`, 4 нишки (Python е едно-нишков).
Скрипт: [realworld/11_bench_3way.py](realworld/11_bench_3way.py); отчита индексиране и
мапване поотделно + памет.

**Цял геном** (6000 прочита/датасет → hs1.fa):

| датасет | mapper | mapped | map% | mapq | общо | индекс | мапване | индекс RAM | пик RAM |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| hifi | cpp | 5991 | 99.85 % | 55.5 | 325 s | 279 s | 41 s | 4.25 GB | 11.2 GB |
| hifi | shmap | 5991 | 99.85 % | 57.0 | 2637 s | — | — | — | 13.5 GB |
| ont | cpp | 5655 | 94.25 % | 52.8 | 1081 s | 347 s | 721 s | 4.25 GB | 11.2 GB |
| ont | shmap | 5750 | 95.83 % | 54.6 | 7795 s | — | — | — | 13.5 GB |
| clr | cpp | 662 | 11.03 % | 8.9 | 314 s | 255 s | 57 s | 4.25 GB | 11.2 GB |
| clr | shmap | 294 | 4.90 % | 44.5 | 1110 s | — | — | — | 13.6 GB |

**Само хромозома** (истинските chr21 прочити → chr21.fa):

| датасет | mapper | reads | mapped | map% | mapq | индекс | мапване | индекс RAM |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| hifi | cpp | 94 | 94 | 100 % | 56.8 | 3.0 s | 0.03 s | 0.12 GB |
| hifi | py | 94 | 94 | 100 % | 56.8 | 10.2 s | 1.5 s | 0.83 GB |
| hifi | shmap | 94 | 94 | 100 % | 58.1 | — | — | — |

**Отговор на „защо не мапва почти всичко?“** С правилната референция мапва: точните
hifi прочити дават **99.85 %** срещу целия геном, а извлечените chr21 прочити — **100 %**
срещу chr21. По-рано срещу самия chr21 се получаваха ~8 %, защото прочитите са whole-genome,
а chr21 е само ~1.5 % от генома — не беше дефект.

**py и cpp са байт-идентични** (еднакви mapped/mapq, потвърдено с `diff`); C++ е ~3–8× по-бърз
от `shmap` на целия геном (hifi 325 s vs 2637 s) и ~25–40× по-бърз от чистия Python.
Индексирането на 3 GB геном отнема ~250–350 s и заема ~4.25 GB (пик ~11 GB по време на build).

**Важно за шумните прочити.** Containment-mapper-ът е надежден за точни прочити (hifi мапва
**пропорционално**: chr21 получава 1.57 % от прочитите ≈ дела си от генома). При шумните
ont (~95 %) и clr (~87 %) обаче whole-genome мапването колабира: прочитите се „залепят“ за
най-големите хромозоми чрез случайни съвпадения на минимайзери (ont: 57 % chr1 + 34 % chr2),
затова истинските chr21 прочити при ont/clr на практика са 0 и хромозомният случай е показан
само за hifi. Това е присъщо ограничение на минималния containment-подход при висок процент
грешки. Synthetic (8000 къси прочита, `k=15 w=11 t=0.5`): py 14 499 r/s, cpp 50 084 r/s, и
двете мапват 3385, placement precision 100 %. CSV:
[realworld/results_rw/](realworld/results_rw/) (`bench_wholegenome_*`, `bench_chromosome_*`).

---

Подробното описание на оригиналния shmap: [ORIGINAL_SHMAP.md](ORIGINAL_SHMAP.md).
Бенчмаркове (само C++ и Python minSHmap): [realworld/](realworld/).

